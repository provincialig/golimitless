version: "2"

run:
  timeout: 3m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - bodyclose
    - zerologlint
    - unparam
    - godox
    - asciicheck
    - dupl
    - goconst
    - gocritic
    - gosec
